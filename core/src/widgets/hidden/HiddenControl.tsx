import type { HiddenField, WidgetControlProps } from '@staticcms/core/interface';
import type { FC } from 'react';

const HiddenControl: FC<WidgetControlProps<string, HiddenField>> = () => {
  return null;
};

export default HiddenControl;
