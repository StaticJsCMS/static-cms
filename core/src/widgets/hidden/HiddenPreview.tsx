import type { HiddenField, WidgetPreviewProps } from '@staticcms/core/interface';
import type { FC } from 'react';

const HiddenPreview: FC<WidgetPreviewProps<string, HiddenField>> = () => {
  return null;
};

export default HiddenPreview;
