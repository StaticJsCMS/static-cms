/* eslint-disable import/prefer-default-export */
import { generateClassNames } from '@staticcms/core/lib/util/theming.util';

export const modalClasses = generateClassNames('Modal', ['root', 'content', 'backdrop']);
